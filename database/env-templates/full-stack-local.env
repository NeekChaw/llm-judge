# ================================================================
# 完整技术栈本地环境（Docker Compose All-in-One）
# ================================================================

# PostgreSQL配置（容器内部）
POSTGRES_PASSWORD=ai_benchmark_postgres_2025

# 应用连接数据库
SUPABASE_URL=postgresql://postgres:ai_benchmark_postgres_2025@postgres:5432/ai_benchmark
SUPABASE_ANON_KEY=local_anon_key_for_development_only
NEXT_PUBLIC_SUPABASE_URL=${SUPABASE_URL}
NEXT_PUBLIC_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}

# Redis配置（容器内部）
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=ai_benchmark_redis_2025

# 加密配置（必填，至少32个字符）
CONFIG_ENCRYPTION_KEY=your_secure_encryption_key_min_32_chars

# Node环境
NODE_ENV=development
